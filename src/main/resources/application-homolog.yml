# ============================================================================
# MOP Client Gateway - Homologation Profile
# ============================================================================
# Configuration for homologation environment
# Activate with: SPRING_PROFILES_ACTIVE=homolog
# All values must be provided via environment variables
# ============================================================================

spring:
  application:
    name: mop-client-gateway
  
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}

  # Spring AMQP Configuration for RabbitMQ
  rabbitmq:
    # Connection settings
    host: ${RABBITMQ_VALIDATOR_HOST}
    port: ${RABBITMQ_VALIDATOR_PORT}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}

    # Listener configuration
    listener:
      simple:
        # Enable automatic acknowledgment
        acknowledge-mode: auto
        # Number of concurrent consumers
        concurrency: ${RABBITMQ_CONCURRENCY}
        # Maximum number of concurrent consumers
        max-concurrency: ${RABBITMQ_MAX_CONCURRENCY}
        # Prefetch count - number of unacknowledged messages per consumer
        prefetch: ${RABBITMQ_PREFETCH}

    # Application-specific RabbitMQ configuration
    queues:
      # Validator queue configuration (for publishing messages)
      validator:
        name: ${RABBITMQ_VALIDATOR_QUEUE_NAME}
      # Output queue configuration (for consuming messages)
      output:
        name: ${RABBITMQ_OUTPUT_QUEUE_NAME}

    # Retry configuration
    retry:
      maxAttempts: ${RABBITMQ_RETRY_MAX_ATTEMPTS}
      backoff: ${RABBITMQ_RETRY_BACKOFF}
      enablesTransactionSupport: ${RABBITMQ_ENABLES_TRANSACTION_SUPPORT}

# ============================================================================
# Server Configuration
# ============================================================================
server:
  port: ${SERVER_PORT}
  servlet:
    context-path: ${SERVER_CONTEXT_PATH}

# ============================================================================
# External API Configuration
# ============================================================================
external:
  server:
    request:
      url: ${EXTERNAL_REQUEST_URL}

# ============================================================================
# Spring Boot Actuator Configuration
# ============================================================================
management:
  endpoints:
    web:
      exposure:
        include: ${MANAGEMENT_ENDPOINTS_INCLUDE}
  endpoint:
    health:
      show-details: ${MANAGEMENT_HEALTH_SHOW_DETAILS}

# ============================================================================
# Logging Configuration
# ============================================================================
logging:
  level:
    root: ${LOG_LEVEL_ROOT}
    br.com.opin.mopclient.gateway: ${LOG_LEVEL_GATEWAY}

